name: Shared Changelog Workflow

on:
  workflow_call:
    inputs:
      # WHY: Keep CLI-backed defaults in the CLI/action. Workflow-call defaults
      # would be forwarded as explicit values and override config-file settings.
      changelog_path:
        required: false
        type: string
      config_path:
        required: false
        type: string
      base_branch:
        required: false
        type: string
      provider:
        required: false
        type: string
      release_tag:
        required: false
        type: string
      release_name:
        required: false
        type: string
      release_body:
        required: false
        type: string
      language:
        required: false
        type: string
      instructions:
        required: false
        type: string
      instructions_file:
        required: false
        type: string
      npm_version:
        required: false
        type: string
      minimum_package_age_days:
        required: false
        type: string
        default: '0'
      dry_run:
        required: false
        type: string
      dry_run_json_report:
        required: false
        type: string
      fail_on_llm_error:
        required: false
        type: string
      require_provider:
        required: false
        type: string
      no_ai:
        required: false
        type: string
    secrets:
      REPO_TOKEN:
        required: false
      CHANGELOG_BOT_APP_ID:
        required: false
      CHANGELOG_BOT_APP_PRIVATE_KEY:
        required: false
      CHANGELOG_BOT_APP_INSTALLATION_ID:
        required: false
      OPENAI_API_KEY:
        required: false
      ANTHROPIC_API_KEY:
        required: false
      GEMINI_API_KEY:
        required: false

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Run changelog bot action
        uses: nyaomaru/changelog-bot@v0
        with:
          changelog-path: ${{ inputs.changelog_path }}
          config-path: ${{ inputs.config_path }}
          base-branch: ${{ inputs.base_branch }}
          provider: ${{ inputs.provider }}
          release-tag: ${{ inputs.release_tag }}
          release-name: ${{ inputs.release_name }}
          release-body: ${{ inputs.release_body }}
          language: ${{ inputs.language }}
          instructions: ${{ inputs.instructions }}
          instructions-file: ${{ inputs.instructions_file }}
          npm-version: ${{ inputs.npm_version }}
          minimum-package-age-days: ${{ inputs.minimum_package_age_days }}
          dry-run: ${{ inputs.dry_run }}
          dry-run-json-report: ${{ inputs.dry_run_json_report }}
          fail-on-llm-error: ${{ inputs.fail_on_llm_error }}
          require-provider: ${{ inputs.require_provider }}
          no-ai: ${{ inputs.no_ai }}
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
          CHANGELOG_BOT_APP_ID: ${{ secrets.CHANGELOG_BOT_APP_ID }}
          CHANGELOG_BOT_APP_PRIVATE_KEY: ${{ secrets.CHANGELOG_BOT_APP_PRIVATE_KEY }}
          CHANGELOG_BOT_APP_INSTALLATION_ID: ${{ secrets.CHANGELOG_BOT_APP_INSTALLATION_ID }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
